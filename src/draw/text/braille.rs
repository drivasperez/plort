use crate::config::PlotType;
use crate::draw::Plot;
use crate::Config;
use crate::DataSet;

pub fn braille_plot(plot: &Plot) {
    todo!()
}

fn get_rows(plot: &Plot) -> Vec<Vec<String>> {
    todo!()
}

fn draw_axes(plot: &Plot, rows: &mut [Vec<String>]) {
    todo!()
}

fn print_header(plot: &Plot, count_mode: bool, columns: usize) {
    todo!()
}

fn plot_points(plot: &Plot, rows: &mut [Vec<String>]) {
    todo!()
}
